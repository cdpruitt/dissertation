Below are the parameter values from our best DOM fits for each nucleus in
the present treatment. The parameter labels correspond to those in the equations
of Chapter \ref{DOMFormalism}. For symmetric nuclei \oSix\ and \caForty, the
asymmetry-dependent parameters were disabled during fitting. As mentioned in
Chapter \ref{DOMResults}, for a few nuclei, one or more parameters were set to
zero to prevent the fit from drifting into unphysical territory, most notably the negative
imaginary surface and real wine-bottle parameters for \oSixEight.
\\
\begin{table}[H]
    \centering
    \caption{Real parameters (volume-like, symmetric)}
    \input{parameters/HFParametersTable.tex}
    \vspace{2em}
    \caption{Real parameters (volume-like, asymmetric)}
    \input{parameters/HFAsymmParametersTable.tex}
\end{table}
\begin{table}[H]
    \centering
    \caption{Imaginary parameters (volume-like, symmetric)}
    \makebox[\textwidth][c]{\input{parameters/ImVolumeParametersTable.tex}}
    \vspace{2em}
    \caption{Imaginary parameters (volume-like, asymmetric)}
    \makebox[\textwidth][c]{\input{parameters/ImVolumeAsymmParametersTable.tex}}
\end{table}
\begin{table}[H]
    \centering
    \caption{Imaginary parameters (surface-like, symmetric)}
    \makebox[\textwidth][c]{\input{parameters/ImSurfaceParametersTable.tex}}
    \vspace{1.5em}
    \caption{Imaginary parameters (surface-like, asymmetric)}
    \makebox[\textwidth][c]{\input{parameters/ImSurfaceAsymmParametersTable.tex}}
    \vspace{1.5em}
    \caption{Spin-orbit parameters}
    \makebox[\textwidth][c]{\input{parameters/SpinOrbitParametersTable.tex}}
\end{table}
